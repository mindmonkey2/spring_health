import 'package:flutter/material.dart';

class TrainerWarmupScreen extends StatelessWidget {
  const TrainerWarmupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Warmup & Adjustments')),
      body: const Center(
        child: Text('Trainer Warmup Screen Stub'),
      ),
    );
  }
}
