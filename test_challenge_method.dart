import 'package:spring_health_member_app/services/challenge_service.dart';
void main() {
  final service = ChallengeService();
  // We want to see if there is any method for fetching members or exercises inside it
}
